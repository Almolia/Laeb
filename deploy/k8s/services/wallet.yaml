apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet
  namespace: laeb
spec:
  replicas: 2
  selector:
    matchLabels: {app: wallet}
  template:
    metadata:
      labels: {app: wallet}
    spec:
      containers:
        - name: wallet
          image: laeb/wallet:latest
          imagePullPolicy: IfNotPresent
          ports:
            - {name: http, containerPort: 8000}
          envFrom:
            - configMapRef: {name: laeb-config}
            - secretRef: {name: laeb-secrets}
          env:
            - name: DATABASE_URL
              valueFrom: {secretKeyRef: {name: laeb-secrets, key: WALLET_DATABASE_URL}}
          livenessProbe:
            httpGet: {path: /health, port: http}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet: {path: /ready, port: http}
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 1000m, memory: 512Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: wallet
  namespace: laeb
spec:
  selector: {app: wallet}
  ports:
    - {name: http, port: 80, targetPort: http}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-worker
  namespace: laeb
spec:
  replicas: 1
  selector:
    matchLabels: {app: wallet-worker}
  template:
    metadata:
      labels: {app: wallet-worker}
    spec:
      containers:
        - name: wallet-worker
          image: laeb/wallet:latest
          imagePullPolicy: IfNotPresent
          command: [python, -m, app.worker]
          envFrom:
            - configMapRef: {name: laeb-config}
            - secretRef: {name: laeb-secrets}
          env:
            - name: DATABASE_URL
              valueFrom: {secretKeyRef: {name: laeb-secrets, key: WALLET_DATABASE_URL}}
          resources:
            requests: {cpu: 50m, memory: 96Mi}
            limits: {cpu: 500m, memory: 384Mi}
