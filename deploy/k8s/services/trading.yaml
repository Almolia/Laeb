apiVersion: apps/v1
kind: Deployment
metadata:
  name: trading
  namespace: laeb
spec:
  replicas: 2
  selector:
    matchLabels: {app: trading}
  template:
    metadata:
      labels: {app: trading}
    spec:
      containers:
        - name: trading
          image: laeb/trading:latest
          imagePullPolicy: IfNotPresent
          ports:
            - {name: http, containerPort: 8000}
          envFrom:
            - configMapRef: {name: laeb-config}
            - secretRef: {name: laeb-secrets}
          env:
            - name: DATABASE_URL
              valueFrom: {secretKeyRef: {name: laeb-secrets, key: TRADING_DATABASE_URL}}
          livenessProbe:
            httpGet: {path: /health, port: http}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet: {path: /ready, port: http}
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 1000m, memory: 512Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: trading
  namespace: laeb
spec:
  selector: {app: trading}
  ports:
    - {name: http, port: 80, targetPort: http}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: trading-worker
  namespace: laeb
spec:
  replicas: 1
  selector:
    matchLabels: {app: trading-worker}
  template:
    metadata:
      labels: {app: trading-worker}
    spec:
      containers:
        - name: trading-worker
          ports:
            - {name: metrics, containerPort: 9107}
          image: laeb/trading:latest
          imagePullPolicy: IfNotPresent
          command: [python, -m, app.worker]
          envFrom:
            - configMapRef: {name: laeb-config}
            - secretRef: {name: laeb-secrets}
          env:
            - name: DATABASE_URL
              valueFrom: {secretKeyRef: {name: laeb-secrets, key: TRADING_DATABASE_URL}}
          resources:
            requests: {cpu: 50m, memory: 96Mi}
            limits: {cpu: 500m, memory: 384Mi}
