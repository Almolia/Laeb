apiVersion: apps/v1
kind: Deployment
metadata:
  name: forum
  namespace: laeb
spec:
  replicas: 2
  selector:
    matchLabels: {app: forum}
  template:
    metadata:
      labels: {app: forum}
    spec:
      containers:
        - name: forum
          image: laeb/forum:latest
          imagePullPolicy: IfNotPresent
          ports:
            - {name: http, containerPort: 8000}
          envFrom:
            - configMapRef: {name: laeb-config}
            - secretRef: {name: laeb-secrets}
          env:
            - {name: MONGO_DB, value: "forum"}
          livenessProbe:
            httpGet: {path: /health, port: http}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet: {path: /ready, port: http}
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 1000m, memory: 512Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: forum
  namespace: laeb
spec:
  selector: {app: forum}
  ports:
    - {name: http, port: 80, targetPort: http}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: forum-worker
  namespace: laeb
spec:
  replicas: 1
  selector:
    matchLabels: {app: forum-worker}
  template:
    metadata:
      labels: {app: forum-worker}
    spec:
      containers:
        - name: forum-worker
          image: laeb/forum:latest
          imagePullPolicy: IfNotPresent
          command: [python, -m, app.worker]
          envFrom:
            - configMapRef: {name: laeb-config}
            - secretRef: {name: laeb-secrets}
          env:
            - {name: MONGO_DB, value: "forum"}
          resources:
            requests: {cpu: 50m, memory: 96Mi}
            limits: {cpu: 500m, memory: 384Mi}
