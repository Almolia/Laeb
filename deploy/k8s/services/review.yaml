apiVersion: apps/v1
kind: Deployment
metadata:
  name: review
  namespace: laeb
spec:
  replicas: 2
  selector:
    matchLabels: {app: review}
  template:
    metadata:
      labels: {app: review}
    spec:
      containers:
        - name: review
          image: laeb/review:latest
          imagePullPolicy: IfNotPresent
          ports:
            - {name: http, containerPort: 8000}
          envFrom:
            - configMapRef: {name: laeb-config}
            - secretRef: {name: laeb-secrets}
          env:
            - {name: MONGO_DB, value: "review"}
          livenessProbe:
            httpGet: {path: /health, port: http}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet: {path: /ready, port: http}
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 1000m, memory: 512Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: review
  namespace: laeb
spec:
  selector: {app: review}
  ports:
    - {name: http, port: 80, targetPort: http}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-worker
  namespace: laeb
spec:
  replicas: 1
  selector:
    matchLabels: {app: review-worker}
  template:
    metadata:
      labels: {app: review-worker}
    spec:
      containers:
        - name: review-worker
          image: laeb/review:latest
          imagePullPolicy: IfNotPresent
          command: [python, -m, app.worker]
          envFrom:
            - configMapRef: {name: laeb-config}
            - secretRef: {name: laeb-secrets}
          env:
            - {name: MONGO_DB, value: "review"}
          resources:
            requests: {cpu: 50m, memory: 96Mi}
            limits: {cpu: 500m, memory: 384Mi}
