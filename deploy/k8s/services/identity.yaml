apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity
  namespace: laeb
spec:
  replicas: 2
  selector:
    matchLabels: {app: identity}
  template:
    metadata:
      labels: {app: identity}
    spec:
      containers:
        - name: identity
          image: laeb/identity:latest
          imagePullPolicy: IfNotPresent
          ports:
            - {name: http, containerPort: 8000}
          envFrom:
            - configMapRef: {name: laeb-config}
            - secretRef: {name: laeb-secrets}
          env:
            - name: DATABASE_URL
              valueFrom: {secretKeyRef: {name: laeb-secrets, key: IDENTITY_DATABASE_URL}}
          livenessProbe:
            httpGet: {path: /health, port: http}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet: {path: /ready, port: http}
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: 1000m, memory: 512Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: identity
  namespace: laeb
spec:
  selector: {app: identity}
  ports:
    - {name: http, port: 80, targetPort: http}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-worker
  namespace: laeb
spec:
  replicas: 1
  selector:
    matchLabels: {app: identity-worker}
  template:
    metadata:
      labels: {app: identity-worker}
    spec:
      containers:
        - name: identity-worker
          image: laeb/identity:latest
          imagePullPolicy: IfNotPresent
          command: [python, -m, app.worker]
          envFrom:
            - configMapRef: {name: laeb-config}
            - secretRef: {name: laeb-secrets}
          env:
            - name: DATABASE_URL
              valueFrom: {secretKeyRef: {name: laeb-secrets, key: IDENTITY_DATABASE_URL}}
          resources:
            requests: {cpu: 50m, memory: 96Mi}
            limits: {cpu: 500m, memory: 384Mi}
